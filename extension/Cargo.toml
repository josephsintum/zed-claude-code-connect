[package]
name = "claude-code-connect-extension"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7.0"
claude-code-connect-resolve = { path = "../resolve" }
