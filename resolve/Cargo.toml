[package]
name = "claude-code-ide-resolve"
version = "0.1.0"
edition = "2021"
description = "Pure binary-resolution rules for the claude-code-ide extension, kept free of the WASM extension API so they can be tested on the host."

[dependencies]
