[package]
name = "zed-claude-ide-resolve"
version = "0.1.0"
edition = "2021"
description = "Pure binary-resolution rules for the zed-claude-ide extension, kept free of the WASM extension API so they can be tested on the host."

[dependencies]
